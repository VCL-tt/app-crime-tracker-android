<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/buttonPressedColor"/>  <!-- Color más oscuro al presionar -->
    <corners android:radius="12dp"/>
    <padding android:left="16dp" android:right="16dp" android:top="12dp" android:bottom="12dp"/>
    <elevation android:value="2dp"/>  <!-- Sombra más suave cuando se presiona -->
</shape>
